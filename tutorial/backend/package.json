{
  "name": "backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../ && yarn run build && cp -r build backend/",
    "deploy": "cd ../../ && git subtree --prefix=tutorial/backend push heroku-tutorial master",
    "deploy:full": "yarn run build:ui && git commit -am uibuild && yarn run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "Justin Arista Kasin",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
